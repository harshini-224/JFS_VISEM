1. git init
2. git add . 
3. git commit -m "git commands"
4. git config --global user.email "your email"
5. git config --global user.name "your name"
6. git remote add origin <url>
7. git branch
8. git push origin main

--------------------------------------
If we add any files (or) change the existing files in the current folder...We need to follow these steps

1. git status //For Verification
2. git add .
3. git commit -m "Changes in commands txt"
4. git push origin main

https://github.com/harshini-224/JFS_VISEM